package com.example.celestic.navigation

/**
 * Simple object containing constant route names used for navigation.
 *
 * This is used for lightweight navigation cases where a sealed class
 * (NavigationRoutes) is not required.
 */
object NavRoutes {
    const val DASHBOARD = "dashboard"
    const val CAMERA = "camera"
    const val STATUS = "status"
}
